[package]
authors = ["Jorge Aparicio <japaricious@gmail.com>"]
build = "build.rs"
name = "trust"
version = "0.1.0"

[build-dependencies]
gcc = "0.3.35"

[dependencies]
# Just to test 'cache: cargo'
clap = "2.13.0"

[features]
c = []
default = ["c"]
